// El unico archivo de configuracion que Tailwind v4 necesita. Los tokens y el
// tema viven en src/app/globals.css, no aqui.
const config = {
  plugins: { "@tailwindcss/postcss": {} },
};

export default config;
