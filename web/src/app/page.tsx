import { Tablero } from "@/components/tablero";

/** La region completa. Cada zona tiene su propia ruta en app/[zona]/page.tsx. */
export default function Pagina() {
  return <Tablero zona={null} />;
}
